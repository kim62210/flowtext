# Changelog

All notable changes to the published Flowtext package are tracked here.

## [Unreleased]

### Added
- initial public package metadata for the Flowtext workspace package
- package-level README, LICENSE, and release-facing changelog files
- build and prepack hooks that rely on the root workspace build pipeline

### Notes
- the repository-level `CHANGELOG.md` remains the more detailed development log during pre-1.0 work
